package com.mknlabs.expensetracker.models

/**
 * Font mode for the app typography.
 *
 * - [APP] — Google Sans (bundled)
 * - [SYSTEM] — Device default (Roboto / OEM)
 * - [CUSTOM] — User-imported TTF/OTF
 */
enum class FontMode {
    APP,
    SYSTEM,
    CUSTOM
}
