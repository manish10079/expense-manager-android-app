<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Item 21 (Option B): OS backup is disabled entirely so the plaintext
         Room DB, DataStores and encrypted PIN store can never be pulled off the
         device via adb backup / cloud restore. Restore paths that remain:
         in-app manual .db export, Firestore sync, and encrypted auto-backups. -->
    <application
        android:name=".ExpenseTrackerApplication"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ExpenseTracker.Splash"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Magic Link Deep Link -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="expense-tracker-2ea00.web.app"
                    android:pathPrefix="/login"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Smart SMS Import: listens for SMS_RECEIVED, delegates to the parser (plan §4/§5) -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Smart SMS Import: one-tap [ Save ] action from the notification -->
        <receiver
            android:name=".sms.SmsActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mknlabs.expensetracker.action.SMS_SAVE" />
            </intent-filter>
        </receiver>

        <!-- Notification analytics: fires when the user dismisses a local notification -->
        <receiver
            android:name=".notifications.NotificationDismissReceiver"
            android:exported="false" />

        <!-- FCM push (notification plan §5.4): token registration + data-only
             message handling. Messages are rendered by NotificationHelper so
             channels/format stay consistent with local alerts. -->
        <service
            android:name=".notifications.ExpenseTrackerMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3052232912913226~7883958511"/>

        <!-- Privacy Policy URL -->
        <meta-data
            android:name="privacy_policy_url"
            android:value="@string/url_privacy_policy" />
    </application>

</manifest>
